# Copy to secrets.toml for local Streamlit runs or paste into Streamlit Cloud secrets.

mongodb_uri = "mongodb+srv://<user>:<password>@<cluster>.mongodb.net/?retryWrites=true&w=majority"

# Optional: require ?debug=<token> to unlock agent diagnostics in the UI.
# diagnostics_token = "your-secret-token"
